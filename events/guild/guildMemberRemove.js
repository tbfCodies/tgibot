const Discord = require("discord.js");
const { checkForUser, saveUserData } = require("../../utils/database/queries/userHandler");

module.exports = {
    name: "guildMemberRemove",
    once: false,

    async execute(member, client) {



    }
}